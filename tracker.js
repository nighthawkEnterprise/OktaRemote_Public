async function uploadImageTracker(msg) {
  
    console.log(msg, ":", config);
}
async function uploadIconTracker(msg) {
    let config = await mainFunction();
    console.log(msg, ":", config);
}

async function errorUploadImageCatchTrack(msg) {
    let config = await mainFunction();
    console.log(msg, ":", config);
}

async function uploadLogoEntryTrack (msg) {
    let config = await mainFunction();
    console.log(msg, ":", config);
}

async function errorUploadLogoCatchTrack(msg) {
    let config = await mainFunction();
    console.log(msg, ":", config);
}

async function onlyUploadImagesTrack(msg) {
    let config = await mainFunction();
    console.log(msg, ":", config);
}

async function onlyUploadLogoTrack(msg) {
    let config = await mainFunction();
    console.log(msg, ":", config);
}

async function tablink2Track(msg) {
    let config = await mainFunction();
    console.log(msg, ":", config);
}

async function tablink3Track(msg) {
    let config = await mainFunction();
    console.log(msg, ":", config);
}
async function tablink4Track(msg) {
    let config = await mainFunction();
    console.log(msg, ":", config);
}
async function saveInputTrack(search) {
    let config = await mainFunction();
    console.log("Background Input Search: ", search);
}
async function saveLogoTrack(search) {
    let config = await mainFunction();
    console.log("Logo Input Search: ", search);
}

async function logoClickedTrack() {
    console.log("LOGO CLICKED");
}
async function defaultImageClickedTrack() {
    console.log("DEFAULT IMAGE CLICKED");
}
async function backgroundImageTrack() {
    console.log("Background Image Clicked");
}


// if it is opened (Done) 
// if a default image gets clicked (done) 
// if a search is done
// if a search picture is clicked
// if a carousel bg image gets uploaded
// if a logo is searched
// if a carousel logo images is clicked
// if a bg image is pasted and uploaded
// error for bg image upload
// if a logo image is pasted and uploaded
// error for logo image upload
// if OIN is searched